// const COMMAND_NAME_REGEX = /^[-_\p{L}\p{N}]{1,32}$/gmu;

export const assertValidCommandName = (name: string) => {
    // if (!COMMAND_NAME_REGEX.test(name))
    //     throw new Error(`Invalid Command Name: ${name}`);
};

export const assertValidCommandParameter = (name: string) => {
    // if (!COMMAND_NAME_REGEX.test(name))
    //     throw new Error(`Invalid Parameter Name: ${name}`);
};
